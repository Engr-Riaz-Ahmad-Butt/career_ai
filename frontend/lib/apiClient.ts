/**
 * @deprecated  Use `lib/api/client.ts` directly.
 * This shim exists so existing imports of `lib/api-client` continue to work
 * while incremental migration to the new endpoint modules is happening.
 */
export { default } from '@/lib/api/client';
